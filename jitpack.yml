install:
#  - ./gradlew :annotation:clean :annotation:build :annotation:install
#  - ./gradlew :processor:clean :processor:build
#  - ./gradlew :utils:build :utils:publishToMavenLocal
  - ./gradlew clean install

